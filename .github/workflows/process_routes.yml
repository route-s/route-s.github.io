name: Process Walking Routes

on:
  push:
    paths:
      - 'routes.geojson'
    branches:
      - main
  workflow_dispatch:

jobs:
  build:
    runs-on: ubuntu-latest
    permissions:
      contents: write

    steps:
      - name: Checkout repository
        uses: actions/checkout@v4

      - name: Set up Python
        uses: setup-python@v5
        with:
          python-version: '3.10'

      - name: Install dependencies
        run: |
          pip install geopandas shapely requests

      - name: Run Spatial Enrichment Script
        run: |
          python process_routes.py

      - name: Commit and push enriched GeoJSON
        run: |
          git config --global user.name 'github-actions[bot]'
          git config --global user.email 'github-actions[bot]@users.noreply.github.com'
          git add routes.geojson
          git diff --quiet && git diff --staged --quiet || (git commit -m "Auto: Enrich spatial admin metadata" && git push)
